# redis.conf

# Disable Append Only File persistence
appendonly no

# Disable RDB snapshots (optional)
save ""

# Set max memory usage for Redis (example: 100mb)
maxmemory 100mb

# Eviction policy: remove least recently used keys when full
maxmemory-policy allkeys-lru
